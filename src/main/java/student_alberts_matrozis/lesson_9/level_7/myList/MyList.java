package student_alberts_matrozis.lesson_9.level_7.myList;

interface MyList {
    // Print all elements of list
    void print();

    // Add an element to list
    void add(Object element);

    // Remove last element of list
    void sub();

    // Clear list, to remove all list elements
    void clear();

    // Returns list length
    int length();
}
